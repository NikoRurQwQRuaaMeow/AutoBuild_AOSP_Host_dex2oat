name: Build Android 16 Host dex2oat

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Maximize Build Space
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 2048
          temp-reserve-mb: 1024
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'

      - name: Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y git-core gnupg flex bison build-essential zip curl zlib1g-dev gcc-multilib g++-multilib libc6-dev-i386 libncurses5 lib32ncurses5-dev x11proto-core-dev libx11-dev lib32z1-dev libgl1-mesa-dev libxml2-utils xsltproc unzip fontconfig python3

      - name: Install Repo
        run: |
          mkdir -p ~/bin
          curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
          chmod a+x ~/bin/repo
          echo "$HOME/bin" >> $GITHUB_PATH

      - name: Repo Init and Sync
        run: |
          mkdir aosp && cd aosp
          repo init -u https://android.googlesource.com/platform/manifest -b master --depth=1
          
          # 同步最核心路径
          repo sync -c -j$(nproc) --no-tags --no-current-branch --no-clone-bundle \
            build/make \
            build/soong \
            build/blueprint \
            art \
            system/core \
            system/libbase \
            external/libcxx \
            external/golang-protobuf \
            external/starlark-go \
            prebuilts/clang/host/linux-x86 \
            prebuilts/build-tools \
            prebuilts/go/linux-x86
          
          rm -rf .repo

      - name: Build dex2oat
        run: |
          cd aosp
          
          # 【终极修复：李代桃僵】
          # 1. 强制创建输出目录（如果不存在）
          mkdir -p out/host/linux-x86/bin/
          
          # 2. 伪造一个 release-config 二进制文件
          # 这个脚本会吞掉所有参数并静默退出，返回 0 (成功)
          echo '#!/bin/bash' > out/host/linux-x86/bin/release-config
          echo 'exit 0' >> out/host/linux-x86/bin/release-config
          chmod +x out/host/linux-x86/bin/release-config
          
          # 3. 告诉环境，如果找不到 vendor，不要报错
          mkdir -p vendor/google/release/
          touch vendor/google/release/release_config_map.textproto

          # 环境变量设置
          export SOONG_ALLOW_MISSING_DEPENDENCIES=true
          export SKIP_VNDK_VAR_CHECK=true
          export TARGET_PRODUCT=aosp_x86_64
          export TARGET_RELEASE=trunk_staging
          export TARGET_BUILD_VARIANT=userdebug
          
          # 尝试直接调用 soong_ui，跳过 envsetup 可能带来的二次检查
          ./build/soong/soong_ui.bash --make-mode dex2oat

      - name: Prepare Artifacts
        run: |
          mkdir -p publish/lib64
          OUT_DIR="aosp/out/host/linux-x86"
          
          if [ -d "$OUT_DIR" ]; then
            # 注意：排除掉我们伪造的那个 release-config
            find $OUT_DIR/bin -name "dex2oat*" -exec cp {} publish/ \;
            LIBS=("libart" "libbase" "libartbase" "libdexfile" "liblz4" "libziparchive" "libz" "liblog" "libc++")
            for lib in "${LIBS[@]}"; do
              find $OUT_DIR/lib64 -name "${lib}.so" -exec cp {} publish/lib64/ \;
            done
          else
            echo "Build failed: No output directory."
            exit 1
          fi

      - name: Upload dex2oat Binary
        uses: actions/upload-artifact@v4
        with:
          name: dex2oat-android16-host
          path: publish/